/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "i.ytimg.com", "img.youtube.com"], // Add any other domains you need here
  },
};

export default nextConfig;
